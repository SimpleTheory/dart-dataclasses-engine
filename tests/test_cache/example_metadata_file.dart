import 'class_functions.dart';


List<ReflectedClass> dataclasses = []; //instantiate here
string2dataclass = dataclasses.map((e) => MapEntry(e.name, e));
string2class = {...string2dataclass, ...string2classdefault}
type2dataclass = {dataclasses.map((e) => MapEntry(e.referenceType, e))};

Map<String, Function> classMapFactory = {
  ...typeFactoryDefault,
  // dataclass factory methods
  // enum factory methods
}



Map<String, ReflectedClass> stringReflectedClass = // instantiate here
List<ReflectedClass> reflectedClasses = List<ReflectedClass>.from(stringRelfectedClass.values)
Map<Type, ReflectedClass> typeReflectedClass =

// cohesion maps
classExtensions
extensionsClasses
typeString
stringType

// Json stuff
Map<String, Function> dataclassFromMap = reflectedClasses.map

Map
